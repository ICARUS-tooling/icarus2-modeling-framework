/**
 * Contains all the members of the query filter framework to pre-process queries and allow
 * efficient filtering before the core of the query engine takes over.
 *
 * @author Markus Gärtner
 *
 */
package de.ims.icarus2.query.api.engine.filter;