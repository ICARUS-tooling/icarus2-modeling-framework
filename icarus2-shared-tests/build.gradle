
description = 'Shared test classes'

dependencies {
	api(project(':icarus2-apiguard'))
}

configurations {
	// external dependencies managed by root build.gradle	
//	api.extendsFrom(junit, mockito, fastutil)
	
	implementation.extendsFrom(javapoet, junit, mockito, fastutil, jmh)
}