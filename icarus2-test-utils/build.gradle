
description = 'Shared test classes'

dependencies {
	api(project(':icarus2-apiguard'))
}

configurations {
	// external dependencies managed by root build.gradle	
	
	implementation.extendsFrom(javapoet, junit, mockito, fastutil, benchmark, assertj, slf4j, apiguardian)
}