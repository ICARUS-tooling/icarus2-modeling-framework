
description = 'Collection of drivers for widely used corpus storage/serialization formats'

// internal dependencies
dependencies {
	api project(':icarus2-file-driver-utils')
	
	testImplementation project(path: ':icarus2-model-api', configuration: 'tests')
}
