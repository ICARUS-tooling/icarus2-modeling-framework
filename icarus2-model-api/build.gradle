
description = 'API for actual corpus modeling and interaction'

// internal dependencies
dependencies {
  api(project(':icarus2-shared-utils'))
  api(project(':icarus2-manifest-api'))
}

configurations {
	// external dependencies managed by root build.gradle
	implementation.extendsFrom(slf4j)
}